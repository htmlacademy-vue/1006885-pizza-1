export const actionTypes = {};

export const gettersTypes = {};

const state = {
  data: null,
  isLoading: false,
  error: null,
};

const getters = {};

const mutations = {};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
